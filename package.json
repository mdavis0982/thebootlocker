{
  "name": "the-boot-locker",
  "version": "1.0.0",
  "description": "Catalogue and admin app for The Boot Locker",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "preview:admin": "node scripts/preview-admin.js",
    "check": "node --check server.js && node --check media.js && node --check public/app.js && node --check public/version-1.js",
    "test": "node --test"
  },
  "dependencies": {
    "cloudinary": "^2.11.0",
    "dotenv": "^16.4.7",
    "express": "^5.2.1",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.0.0",
    "multer": "^2.3.0",
    "pg": "^8.13.1"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8",
    "jsdom": "^26.1.0"
  }
}
